import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vorkout.app',
  appName: 'Vorkout',
  webDir: 'dist/spa',
  bundledWebRuntime: false
};

export default config;
