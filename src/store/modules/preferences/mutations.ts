import { MutationTree } from 'vuex';
import { PreferencesInterface } from './state';

const mutation: MutationTree<PreferencesInterface> = {
  someMutation (/* state: ExampleStateInterface */) {
    // your code
  }
};

export default mutation;
